{
  "name": "devtribe-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && rm -rf node_modules package-lock.json && npm install && npm run build",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
