{
  "name": "devtribe-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
